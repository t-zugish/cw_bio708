library(swirl)
library(tibble)

tibble(
  x = letters[1:5],
  y = 1:5
)

#swirl prompt: Create a tibble with column x with lowercase alphabet a to e, and with column y with integers 1 to 5.
#seems like it defaults to columns rather than rows? 

